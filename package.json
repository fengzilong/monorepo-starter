{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "coverage": "turbo coverage",
    "prepare": "simple-git-hooks",
    "publish-packages": "turbo build && turbo lint lint-packages test && changeset version && changeset publish",
    "ci:publish-packages": "turbo build && turbo lint lint-packages test && changeset publish",
    "plop": "plop --plopfile plopfile.mts"
  },
  "simple-git-hooks": {
    "pre-commit": "turbo build && turbo lint lint-packages test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.11",
    "simple-git-hooks": "^2",
    "turbo": "^2.7.2",
    "plop": "^4.0.4"
  },
  "packageManager": "pnpm@10.29.1",
  "engines": {
    "node": "^20.0.0 || ^22.0.0 || >=24.0.0"
  }
}